# Installed skills

Vendored Claude Code project skills (auto-discovered from `.claude/skills/<name>/SKILL.md`).

| Skill(s) | Source | License |
|---|---|---|
| `banner-design`, `brand`, `design`, `design-system`, `slides`, `ui-styling`, `ui-ux-pro-max` | [nextlevelbuilder/ui-ux-pro-max-skill](https://github.com/nextlevelbuilder/ui-ux-pro-max-skill) | MIT (`_licenses/ui-ux-pro-max-skill-LICENSE.txt`) |
| `impeccable` | [pbakaus/impeccable](https://github.com/pbakaus/impeccable) | Apache-2.0 (`_licenses/impeccable-LICENSE.txt`) |

Not installed: the `frontend-design` plugin from https://claude.com/plugins/frontend-design. That page lives on `claude.com`, which this session's network policy blocks, and it isn't a plain git skill repo — it's a claude.ai/Claude Code account-level plugin, installed via `/plugin marketplace add` + `/plugin install` (or the claude.ai plugin UI), not by vendoring files into a repo. Searching the connected plugin catalog for "frontend-design" / "frontend design" / "UI design" turned up no exact match, so it needs to be installed manually — see the page for its marketplace/install instructions.
